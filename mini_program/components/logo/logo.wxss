/* components/logo/logo.wxss */
.logo {
    text-align: center;
    font-size: 20rpx;
    background-color: rgba(250, 250, 250, 0.7);
    color: #ccc;
    padding-bottom: 20rpx;
}

.logo image {
    width: 50rpx;
    height: 50rpx;
}
